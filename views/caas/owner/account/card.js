exports.add = function(req, res) {
  // Add card and set as current if marked or no other cards
  // exist
};

exports.change = function(req, res) {
  // Change which card to use
};

exports.remove = function(req, res) {
  // If selected card is current
    // Reject
  // Else
    // Delete card
};

exports.pay = function(req, res) {
  // Get one time single token
  // Apply to current billing.
};